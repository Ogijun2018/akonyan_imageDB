<h1>Pictures#new</h1>
<%= form_with(model: @picture, url: pictures_path, method: :put, local: true) do |f| %>
  <% if @picture %>
    画像が登録されています。
  <% else %>
     画像が登録されていません。
  <% end %>
  <%= f.file_field :image %>
  <%= f.submit '画像をアップロードする' %>
<% end %>